# Webull Authentication (choose one method)
# Method 1: Email and Password
WEBULL_EMAIL=your_email@example.com
WEBULL_PASSWORD=your_password

# Method 2: Token Authentication (more secure)
# WEBULL_TOKEN=your_long_lived_token

# Kill Switch Configuration
PNL_THRESHOLD=-500  # -$500 P/L threshold (negative number for loss)
THRESHOLD_TYPE=DOLLAR  # Use DOLLAR for $ amount or PERCENT for percentage
CHECK_INTERVAL=60    # Check every 60 seconds
KILL_SCRIPT_PATH=~/killTradingApp.scpt  # Path to AppleScript 