# Example environment file for webull-killswitch
# Copy this file to .env and replace with your actual values

# Webull Authentication (choose one method)
# Method 1: Email and Password
WEBULL_EMAIL=your_email@example.com
WEBULL_PASSWORD=your_password

# Method 2: Token Authentication (more secure)
# WEBULL_TOKEN=your_long_lived_token
# WEBULL_DEVICE_ID=your_device_id
# WEBULL_ACCOUNT_ID=your_account_id

# Kill Switch Configuration
PNL_THRESHOLD=-650     # Trigger at -$650 loss (use negative number)
THRESHOLD_TYPE=DOLLAR  # Use DOLLAR for $ amount or PERCENT for percentage
CHECK_INTERVAL=60      # Check every 60 seconds
KILL_SCRIPT_PATH=killTradingApp.scpt  # Path to AppleScript that kills the trading app

# Notification Settings (optional)
# NOTIFICATION_ENABLED=true
# NOTIFICATION_SOUND=Glass 